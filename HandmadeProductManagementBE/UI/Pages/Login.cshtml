@page
@model LoginModel
<div class="font-[sans-serif]">
    <div class="fle-col flex min-h-screen items-center justify-center px-4 py-6">
        <div class="grid w-full max-w-6xl items-center gap-4 md:grid-cols-2">
            <div class="shadow-[0_2px_22px_-4px_rgba(93,96,127,0.2)] max-w-md rounded-lg border border-gray-300 p-6 max-md:mx-auto">
                <form method="post" class="space-y-4">
                    <div class="mb-8">
                        <h3 class="text-3xl font-extrabold text-gray-800">Sign in</h3>
                        <p class="mt-4 text-sm leading-relaxed text-gray-500">Sign in to your account and explore a world of possibilities. Your journey begins here.</p>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                    {
                        <div class="text-red-500">@Model.ErrorMessage</div>
                    }

                    <div>
                        <label class="mb-2 block text-sm text-gray-800">Email address</label>
                        <div class="relative flex items-center">
                            <input asp-for="Email" type="email" required class="w-full rounded-lg border border-gray-300 px-4 py-3 text-sm text-gray-800 outline-blue-600" placeholder="Enter email address" />
                        </div>
                        <span asp-validation-for="Email" class="text-red-500"></span>
                    </div>

                    <div>
                        <label class="mb-2 block text-sm text-gray-800">Password</label>
                        <div class="relative flex items-center">
                            <input asp-for="Password" type="password" required class="w-full rounded-lg border border-gray-300 px-4 py-3 text-sm text-gray-800 outline-blue-600" placeholder="Enter password" />
                        </div>
                        <span asp-validation-for="Password" class="text-red-500"></span>
                    </div>

                    <div class="!mt-8">
                        <button type="submit" class="w-full rounded-lg bg-blue-600 px-4 py-3 text-sm tracking-wide text-white shadow-xl hover:bg-blue-700 focus:outline-none">
                            Log in
                        </button>
                    </div>

                    <p class="!mt-8 text-center text-sm text-gray-800">Don't have an account? <a href="/Registration" class="ml-1 whitespace-nowrap font-semibold text-blue-600 hover:underline">Register here</a></p>
                </form>
            </div>
            <div class="md:h-[300px] lg:h-[400px] max-md:mt-8">
                <img src="https://readymadeui.com/login-image.webp" class="mx-auto block h-full w-full object-cover max-md:w-4/5" alt="Login" />
            </div>
        </div>
    </div>
</div>
